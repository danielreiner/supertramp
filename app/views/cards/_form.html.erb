<%= simple_form_for(@card, :url=>theme_cards_path(@theme)) do |f| %>
  
  <%= f.input :title, :label => "Kartenname", :input_html => { :class => "span4" } %>
  <%= f.submit "Karte anlegen", :class => "primary" %>
<% end %>
